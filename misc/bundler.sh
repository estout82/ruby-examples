
bundle install                              # install all deps in gemfile
bundle update                               # update gems based on versions listed in gemfile
bundle lock --add-platform x86_64-linux     # add a platform to the lock file (default is only your platform..)